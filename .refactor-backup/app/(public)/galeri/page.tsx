"use client";
import { useState } from "react";
import { X } from "lucide-react";
import { SiteHeader } from "@/components/layout/site-header";
import { Footer } from "@/components/layout/footer";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { departments, gallery } from "@/data/mock-data";
import type { GalleryItem } from "@/types";
export default function GalleryPage() { const [dept, setDept] = useState("semua"); const [type, setType] = useState("semua"); const [selected, setSelected] = useState<GalleryItem | null>(null); const items = gallery.filter((item) => (dept === "semua" || item.department === dept) && (type === "semua" || item.type === type)); return <><SiteHeader /><main className="container-app py-14 sm:py-18"><div className="text-center"><p className="eyebrow">Galeri siswa</p><h1 className="mt-3 font-heading text-4xl font-bold tracking-[-.05em] sm:text-5xl">Karya lahir dari rasa ingin tahu.</h1><p className="mx-auto mt-4 max-w-2xl text-[#6d6f69]">Temukan proyek, pertunjukan, dan dokumentasi proses belajar dari berbagai program studi.</p></div><div className="mt-10 flex flex-col justify-between gap-3 rounded-2xl border border-[#e5e5df] bg-white p-3 sm:flex-row"><div className="flex gap-2 overflow-x-auto">{[{ slug: "semua", shortName: "Semua" }, ...departments].map((d) => <button key={d.slug} onClick={() => setDept(d.slug)} className={`whitespace-nowrap rounded-xl px-3 py-2 text-xs font-semibold ${dept === d.slug ? "bg-[#315c4c] text-white" : "hover:bg-[#f1f2ee]"}`}>{d.shortName}</button>)}</div><select value={type} onChange={(e) => setType(e.target.value)} className="rounded-xl border border-[#e1e1dc] bg-white px-3 py-2 text-xs font-semibold outline-none"><option value="semua">Semua jenis karya</option><option>Karya digital</option><option>Pertunjukan</option><option>Proyek</option><option>Dokumentasi</option></select></div><div className="mt-8 columns-1 gap-5 sm:columns-2 lg:columns-3">{items.map((item, index) => <button key={item.id} onClick={() => setSelected(item)} className="group mb-5 block w-full break-inside-avoid overflow-hidden rounded-2xl border border-[#e5e5df] bg-white text-left"><img src={item.image} alt={item.title} className={`w-full object-cover transition duration-500 group-hover:scale-[1.02] ${index % 3 === 1 ? "aspect-[4/5]" : "aspect-[4/3]"}`} /><div className="p-5"><Badge>{item.type}</Badge><h2 className="mt-3 font-heading text-lg font-bold">{item.title}</h2><p className="mt-1 text-sm text-[#777]">{item.student} · {item.year}</p></div></button>)}</div></main>{selected && <div className="fixed inset-0 z-[70] grid place-items-center bg-black/45 p-4" onClick={() => setSelected(null)}><div className="relative max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-[24px] bg-white" onClick={(e) => e.stopPropagation()}><Button variant="secondary" size="icon" className="absolute right-4 top-4 z-10" onClick={() => setSelected(null)}><X /></Button><img src={selected.image} alt={selected.title} className="aspect-[16/9] w-full object-cover" /><div className="p-6 sm:p-8"><Badge>{selected.type}</Badge><h2 className="mt-3 font-heading text-2xl font-bold">{selected.title}</h2><p className="mt-2 text-sm text-[#777]">{selected.student} · {selected.year}</p><p className="mt-5 text-sm leading-7 text-[#5f615c]">{selected.description}</p></div></div></div>}<Footer /></>; }
