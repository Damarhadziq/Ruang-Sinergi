import Link from "next/link";
import { ArrowUpRight, CarFront, CircuitBoard, Code2, Dumbbell, HeartHandshake, Sparkles } from "lucide-react";
import type { Department } from "@/types";
const icons = { Code2, HeartHandshake, Dumbbell, Sparkles, CircuitBoard, CarFront };
export function DepartmentCard({ department }: { department: Department }) { const Icon = icons[department.icon as keyof typeof icons]; return <Link href={`/prodi/${department.slug}`} className="group rounded-xl py-6 transition-colors hover:bg-[#fcfcfc] sm:px-5"><div className="flex items-start gap-4"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl" style={{ backgroundColor: department.soft, color: department.color }}><Icon size={21} /></span><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><h3 className="font-heading text-lg font-bold">{department.shortName}</h3><ArrowUpRight size={17} className="shrink-0 text-[var(--muted-foreground)]" /></div><p className="mt-2 text-sm leading-6 text-[var(--muted-foreground)]">{department.description}</p><p className="mt-3 text-sm font-semibold" style={{ color: department.color }}>{department.stats.materials} materi tersedia</p></div></div></Link>; }
