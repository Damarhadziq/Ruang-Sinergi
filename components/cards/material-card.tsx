import Link from "next/link";
import { ArrowUpRight, Clock } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { getDepartment } from "@/data/mock-data";
import type { Material } from "@/types";
export function MaterialCard({ material, compact = false }: { material: Material; compact?: boolean }) { const dept = getDepartment(material.department)!; return <Link href={`/materi/${material.slug}`} className="group block overflow-hidden rounded-2xl border border-[var(--border)] bg-[var(--surface)] transition hover:-translate-y-0.5 hover:border-[#aebbb3]"><div className={compact ? "flex gap-4 p-4" : ""}>{!compact && <img src={material.image} alt={`Pratinjau materi ${material.title}`} className="aspect-[16/10] w-full object-cover" />}{compact && <img src={material.image} alt="" className="h-24 w-28 shrink-0 rounded-xl object-cover" />}<div className={compact ? "min-w-0 flex-1" : "p-5"}><div className="mb-3 flex items-center justify-between gap-2"><Badge style={{ backgroundColor: dept.soft, color: dept.color }}>{dept.shortName}</Badge><ArrowUpRight size={17} className="text-[var(--muted-foreground)]" /></div><h3 className="font-heading text-lg font-bold leading-snug">{material.title}</h3>{!compact && <p className="mt-2 line-clamp-2 text-sm leading-6 text-[var(--muted-foreground)]">{material.summary}</p>}<div className="mt-4 flex flex-wrap items-center gap-2 text-xs text-[var(--muted-foreground)]"><span>{material.type}</span><span aria-hidden="true">·</span><span className="flex items-center gap-1"><Clock size={13} />{material.duration} menit</span><span aria-hidden="true">·</span><span>{material.difficulty}</span></div></div></div></Link>; }
