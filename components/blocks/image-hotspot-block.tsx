"use client";
import { useState } from "react";
export function ImageHotspotBlock({ image, points }: { image: string; points: { x: number; y: number; name: string; description: string }[] }) { const [active, setActive] = useState(0); return <div className="block-surface"><h3 className="block-title">Kenali komponen mesin</h3><p className="mt-1 text-sm text-[#6d6e68]">Ketuk titik pada gambar untuk melihat fungsinya.</p><div className="relative mt-4 overflow-hidden rounded-2xl bg-[#eee]"><img src={image} alt="Ruang mesin kendaraan" className="aspect-[16/9] w-full object-cover" />{points.map((point, index) => <button key={point.name} onClick={() => setActive(index)} aria-label={point.name} className="absolute grid h-8 w-8 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full border-4 border-white bg-[#315c4c] text-xs font-bold text-white transition" style={{ left: `${point.x}%`, top: `${point.y}%` }}>{index + 1}</button>)}</div><div className="mt-3 rounded-xl bg-[#edf3f0] p-4"><p className="font-heading font-bold text-[#315c4c]">{points[active].name}</p><p className="mt-1 text-sm leading-6 text-[#5f6863]">{points[active].description}</p></div></div>; }

